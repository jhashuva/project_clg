CREATE TABLE STUDENT(
ROLL_NUMBER VARCHAR2(10) PRIMARY KEY,
FULL_NAME VARCHAR2(30) NOT NULL,
DEPT_ID VARCHAR2(2) NOT NULL,
MOBILE NUMBER(10),
EMAIL VARCHAR2(20),
ADDRESS VARCHAR2(70),
DOB DATE,
BLOOD_GROUP VARCHAR2(5),
YOJ DATE,
RANK NUMBER(8),
AADHAR NUMBER(15)
)
